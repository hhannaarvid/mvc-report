{% extends "base.html.twig" %}

{% block title %}Home{% endblock %}


{% block body %}
<h1 class="article-h1">Kortlek blandad</h1>

    <div class="article-report">

        <div class="full-deck">



            {% for card in shuffled %}
            {# <p>{{card}}</p>  #}
                <img src="{{ asset('img/deck/'~card~'.svg') }}" alt="{{card}}" class="card-deck">
            {% endfor %} 


        </div>
        <br>
        <br>
    </div>
    <br>
        <br>
{% endblock %}