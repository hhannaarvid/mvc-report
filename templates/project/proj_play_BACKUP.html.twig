{% extends "project/base2.html.twig" %}

{% block title %}Home{% endblock %}


{% block body %}
<h1 class="article-h1">Black Jack</h1>

    <div class="article-game">

        
            <div class="gamedraw">
                <div class="gameinfo">
                    <p>Spelare: {{userOne}}</p>
                    <p>Ditt kort:</p><br>
                    <p>Din totala poäng: {{userpoints}}</p>
                </div>
                <div class="cards">
                {% for card in cardhand %}
                {# {{card}} #}
                <img src="{{ asset('img/deck/'~card~'.svg') }}" alt="{{card}}" class="card-draw"><br>
                {% endfor %}
            </div>
            </div>
        
        <br>
        <div class="gamediv">
            <form method="post" action="">
                <input type="submit" name="draw" value="Dra ett kort" class="gamebtn"
                    onClick='this.form.action="{{ path("proj_play") }}";'>

                <input type="submit" name="save" value="Spara poäng" class="gamebtn"
                    onClick="this.form.action='{{ path('proj_save') }}';">

                <input type="submit" name="quit" value="Spela igen" class="gamebtn"
                    onClick="this.form.action='{{ path('proj') }}';this.form.method='get';">

            </form>
        </div>

        <br>
    </div>
    <br>
        <br>
{% endblock %}