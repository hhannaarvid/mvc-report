{% extends "project/base2.html.twig" %}

{% block title %}Home{% endblock %}


{% block body %}
<h1 class="article-h1">Black Jack</h1>

    <div class="article-game">

        
            <div class="gamedraw">
                <div class="gameinfo">
                    <p>Spelare: {{userOne}}</p>     
                    <p>Ditt kort:</p><br>
                    <p>Din totala poäng: {{userpoints}}</p>
                </div>
                {% for card in cardhand %}
                <img src="{{ asset('img/deck/'~card~'.svg') }}" alt="{{card}}" class="card-draw"><br>
                {% endfor %}
            </div>
        
        <br>
        <div class="gamediv">
            <form method="post" action="">
                <input type="submit" name="draw" value="Dra ett kort" class="gamebtn"
                    onClick="this.form.action='{{ path('proj_play') }}';">

            </form>
        </div>

        <br>
    </div>
    <br>
        <br>
{% endblock %}