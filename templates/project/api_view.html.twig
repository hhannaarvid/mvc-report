{% extends "project/base2.html.twig" %}

{% block title %}Projekt{% endblock %}


{% block body %}
<h1 class="article-h1">JSON API</h1>
    <div class="article-report">
        <p>
            Tips! För att se alla JSON-routes är det bäst att ha spelat någon omgång av blackjack först, då flera av sidorna använder data/statistik därifrån.<br>
            
            <br>
            
            JSON-routes:<br>
            <a href="{{ path('game_deck') }}">/proj/gamedeck</a> : Aktuell kortlek, blandad.<br>
            <a href="{{ path('api_proj') }}">/api/proj</a> : Aktuell speldata från blackjack<br>
            <a href="{{ path('api_money') }}">/api/proj/money</a> : Aktuella vinster/insättningar i blackjack<br>
            /api/proj/{player} : visa data för en spelare<br>
            <a href="{{ path('api_player', {'player': 1}) }}">/api/proj/1</a> : Speldata för val vald spelare (exempel)<br>
            <br>
            Dra starthänder för antal spelare (1-3):
        <form method="post" action="{{ path('api_start') }}">
            <label for="start">Antal spelare:</label>
            <input type="number" id="start" name="start" min="1" max="3" required>
            <input type="submit" value="Spara" class="projbtn">
        </form>

        </p>

</p>



    </div>
{% endblock %}